#include <bits/stdc++.h>
using namespace std;


  
int main()
{
  pair < long long int , long long int > p;
  while(cin >> p.first >> p.second)
  {
    
    long long int ans = (p.first-1)+(p.first*(p.second-1));
    cout << ans << "\n";
  }
    
    
    
  
  
}